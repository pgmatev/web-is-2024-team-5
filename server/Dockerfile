FROM node:20-alpine

WORKDIR /app/server
COPY package*.json ./
RUN npm install
COPY . .

EXPOSE 3000
ARG MONGODB_URI
ENV MONGODB_URI=${MONGODB_URI}
ENV JWT_SECRET=${JWT_SECRET}
ENV JWT_EXPIRE=${JWT_EXPIRE}

CMD ["npm", "start"]
